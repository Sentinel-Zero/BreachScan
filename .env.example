# BreachScan example environment file
# Copy to `.env` at the project root and fill in real values.
# DO NOT COMMIT a populated `.env` containing secrets.

# --- Tenable API Credentials (for future real integration) ---
# These are used by `Settings` in `breachscan_backend/app/config.py`.
TENABLE_ACCESS_KEY=your_access_key_here
TENABLE_SECRET_KEY=your_secret_key_here
# Base URL for Tenable.io; change if pointing to a different environment or proxy.
TENABLE_BASE_URL=https://cloud.tenable.com

# --- Application Settings ---
# Host/port passed to uvicorn or other runner.
APP_HOST=0.0.0.0
APP_PORT=8000
# Logging level suggestion (info | debug | warning | error)
LOG_LEVEL=info

# --- Scheduling / Scan Expansion Limits ---
# Maximum number of discrete IPs produced after expanding CIDR/range tokens.
SCHEDULE_EXPANSION_LIMIT=4096

# --- Timezone / Display (future use) ---
# TZ=UTC

# --- Misc Placeholders for future features ---
# DATABASE_URL=sqlite:///./breachscan.db
# REDIS_URL=redis://localhost:6379/0
# AUTH_TOKEN_SECRET=change_me_for_jwt

# After editing, restart the backend so pydantic BaseSettings reloads values.
